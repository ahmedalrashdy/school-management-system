# تبويب الحضور والغياب في ملف الطالب

## نظرة عامة

تبويب شامل لعرض سجل الحضور والغياب الفردي لكل طالب في النظام، مع إحصائيات تفصيلية، عرض تقويمي تفاعلي، وسجل مفصل لكل حصة.

## الوصول للتبويب

**المسار:** `/dashboard/students/{student}/show` → تبويب "الحضور"

## المكونات الرئيسية

### 1. بطاقات الملخص (Summary Cards)

#### الموقع

أعلى التبويب مباشرة

#### الهدف

تقديم لمحة سريعة عن أداء الطالب خلال فترة زمنية محددة

#### فلتر الفترة الزمنية

قائمة منسدلة في أعلى يمين القسم مع الخيارات التالية:

-   **هذا الأسبوع** (`week`): من الأحد إلى السبت الحالي
-   **هذا الشهر** (`month`): من بداية الشهر حتى اليوم
-   **هذا الفصل الدراسي** (`term`) - **الافتراضي**: من بداية السنة الدراسية حتى اليوم

#### البطاقات الأربعة

##### 1. نسبة الحضور

-   **اللون**: أخضر (Success)
-   **الأيقونة**: `fa-percentage`
-   **القيمة**: نسبة مئوية (مثال: 95.2%)
-   **التفاصيل**: "من X حصة"
-   **الحساب**: `(عدد الحصص الحاضر فيها / إجمالي الحصص) × 100`

##### 2. أيام الغياب

-   **اللون**: أحمر (Danger)
-   **الأيقونة**: `fa-calendar-times`
-   **القيمة**: عدد صحيح
-   **التفاصيل**: "يوم دراسي"
-   **الحساب**: عدد الأيام الفريدة التي غاب فيها الطالب عن حصة واحدة على الأقل

##### 3. عدد مرات التأخير

-   **اللون**: برتقالي (Warning)
-   **الأيقونة**: `fa-clock`
-   **القيمة**: عدد صحيح
-   **التفاصيل**: "حصة"
-   **الحساب**: إجمالي الحصص المسجلة كـ "متأخر"

##### 4. غياب بدون عذر

-   **اللون**: أحمر داكن (Red)
-   **الأيقونة**: `fa-exclamation-triangle`
-   **القيمة**: عدد صحيح
-   **التفاصيل**: "حصة"
-   **الحساب**: عدد الحصص المسجلة كـ "غائب" (باستثناء "معذور")

### 2. عرض التقويم (Calendar View)

#### الهدف

توفير نظرة بصرية سريعة على حضور الطالب خلال شهر كامل

#### التنقل

-   **زر السابق** (`←`): الانتقال للشهر السابق
-   **اسم الشهر**: عرض الشهر والسنة الحالية بالعربي
-   **زر التالي** (`→`): الانتقال للشهر التالي

#### تلوين الأيام

| الحالة           | اللون      | المعنى                                  |
| ---------------- | ---------- | --------------------------------------- |
| **حضور كامل**    | أخضر فاتح  | حضر جميع حصص اليوم                      |
| **حضور جزئي**    | أصفر       | غاب عن حصة واحدة على الأقل أو تأخر      |
| **غياب كامل**    | أحمر فاتح  | غاب عن جميع حصص اليوم                   |
| **عطلة أسبوعية** | رمادي      | الجمعة والسبت                           |
| **شهر مختلف**    | رمادي فاتح | أيام من الشهر السابق/اللاحق في نفس View |

#### التفاعل

##### Hover (المرور فوق اليوم)

يظهر Tooltip يحتوي على:

```
حاضر: X | متأخر: Y | غائب: Z
```

##### البيانات المعروضة داخل اليوم

-   **رقم اليوم** (أعلى)
-   **أيقونات صغيرة**:
    -   ✓ X (أخضر) - عدد الحصص الحاضر فيها
    -   🕐 Y (برتقالي) - عدد الحصص المتأخر فيها
    -   ✗ Z (أحمر) - عدد الحصص الغائب فيها

##### Click (النقر على اليوم)

-   يتم تحديث **السجل التفصيلي** (المكون الثالث) لعرض حصص هذا اليوم فقط
-   يظهر banner في أسفل التقويم:
    ```
    تم الفلترة للتاريخ: [اسم التاريخ] [زر إلغاء الفلتر]
    ```

#### Legend (مفتاح الألوان)

صف في أسفل التقويم يوضح معنى كل لون

### 3. السجل التفصيلي للحصص (Detailed Sessions Log)

#### الهدف

عرض قائمة تاريخية دقيقة لكل حصة وحالة حضور الطالب فيها

#### الفلاتر

##### فلتر المادة

-   قائمة منسدلة تعرض جميع المواد التي يدرسها الطالب
-   **الافتراضي**: "جميع المواد"
-   **التطبيق**: فوري (Live)

##### فلتر الحالة

-   قائمة منسدلة بحالات الحضور:
    -   حاضر
    -   غائب
    -   متأخر
    -   معذور
-   **الافتراضي**: "جميع الحالات"
-   **التطبيق**: فوري (Live)

##### فلتر التاريخ (من التقويم)

-   يتم تطبيقه تلقائياً عند النقر على يوم في التقويم
-   **الافتراضي**: آخر 30 يوم
-   يمكن إلغاؤه من Banner أسفل التقويم

##### زر إعادة التعيين

-   يظهر فقط عند تفعيل أي فلتر
-   يعيد جميع الفلاتر للقيم الافتراضية

#### الجدول

##### الأعمدة

| العمود             | المحتوى                         | الملاحظات         |
| ------------------ | ------------------------------- | ----------------- |
| **التاريخ واليوم** | - التاريخ الكامل<br>- اسم اليوم | بصيغة عربية كاملة |
| **الحصة**          | رقم الحصة                       | مع أيقونة ساعة    |
| **المادة**         | اسم المادة                      | -                 |
| **المدرس**         | الاسم الكامل للمدرس             | -                 |
| **الحالة**         | Badge ملون                      | حسب حالة الحضور   |
| **الملاحظات**      | نص حر                           | يعرض "-" إذا فارغ |

##### تنسيق Badge الحالة

| الحالة | اللون   | الأيقونة          |
| ------ | ------- | ----------------- |
| حاضر   | أخضر    | `fa-check-circle` |
| متأخر  | برتقالي | `fa-clock`        |
| غائب   | أحمر    | `fa-times-circle` |
| معذور  | أزرق    | `fa-file-medical` |

#### الترتيب

-   **الافتراضي**: الأحدث أولاً (Descending بـ ID)
-   الحصص الأخيرة تظهر في الأعلى

#### Pagination

-   20 حصة لكل صفحة
-   استخدام Livewire Pagination
-   أزرار التنقل في الأسفل

#### Empty State

عند عدم وجود سجلات:

```
[أيقونة صندوق فارغ]
لا توجد سجلات حضور
```

## التدفق التقني

### Data Flow

```
Mount (عند تحميل الصفحة)
  ↓
loadSummaryStats() → حساب الإحصائيات حسب الفترة المختارة
  ↓
loadCalendarData() → تحميل بيانات التقويم للشهر الحالي
  ↓
render() → تحميل السجل التفصيلي (آخر 30 يوم)
```

### Events & Interactions

```
تغيير فلتر الفترة (summaryPeriod)
  ↓
updatedSummaryPeriod()
  ↓
loadSummaryStats() → إعادة حساب

---

تغيير الشهر (previousMonth/nextMonth)
  ↓
تحديث calendarMonth
  ↓
loadCalendarData() → تحميل بيانات الشهر الجديد

---

النقر على يوم في التقويم
  ↓
selectDate(date)
  ↓
تحديث selectedDate
  ↓
resetPage() → إعادة تعيين Pagination
  ↓
render() → تصفية السجل لهذا اليوم فقط

---

تغيير فلتر المادة/الحالة
  ↓
updatedFilterSubjectId() / updatedFilterStatus()
  ↓
resetPage()
  ↓
render() → تطبيق الفلاتر
```

## الاستعلامات (Queries)

### Summary Stats Query

```php
Attendance::where('student_id', $student->id)
    ->whereHas('classSession', function ($q) use ($dateRange) {
        $q->whereBetween('session_date', [$start, $end]);
    })
    ->with('classSession')
    ->get();
```

### Calendar Data Query

```php
Attendance::where('student_id', $student->id)
    ->whereHas('classSession', function ($q) use ($month) {
        $q->whereBetween('session_date', [$startOfMonth, $endOfMonth]);
    })
    ->with('classSession')
    ->get()
    ->groupBy('session_date');
```

### Detailed Log Query

```php
Attendance::where('student_id', $student->id)
    ->with([
        'classSession.timetableSlot.teacherAssignment.curriculumSubject.subject',
        'classSession.timetableSlot.teacherAssignment.teacher.user',
    ])
    // Filters applied
    ->orderBy('id', 'desc')
    ->paginate(20);
```

## حالات الاستخدام

### 1. ولي الأمر

-   **الهدف**: متابعة حضور ابنه
-   **الاستخدام**:
    1. عرض نسبة الحضور الشهرية
    2. فحص التقويم لمعرفة أيام الغياب
    3. النقر على يوم معين لرؤية تفاصيل ذلك اليوم

### 2. المدرس

-   **الهدف**: مراجعة سجل طالب معين
-   **الاستخدام**:
    1. فلترة السجل حسب مادته فقط
    2. التحقق من أنماط الغياب
    3. قراءة الملاحظات السابقة

### 3. مشرف الحضور

-   **الهدف**: تقييم شامل لالتزام الطالب
-   **الاستخدام**:
    1. مقارنة الإحصائيات بين فترات مختلفة (أسبوع/شهر/فصل)
    2. فلترة حالات الغياب فقط
    3. تحديد الأنماط المتكررة

### 4. مدير المدرسة

-   **الهدف**: اتخاذ قرارات بناءً على الحضور
-   **الاستخدام**:
    1. عرض نسبة الحضور للفصل الدراسي
    2. التحقق من عدد الغياب بدون عذر
    3. مراجعة سجل الطالب قبل اجتماع مع ولي الأمر

## الميزات التقنية

### Performance Optimizations

-   ✅ **Eager Loading**: تحميل العلاقات مسبقاً لتجنب N+1 queries
-   ✅ **Pagination**: 20 سجل لكل صفحة
-   ✅ **Lazy Loading**: البيانات تُحمل فقط عند الحاجة
-   ✅ **Efficient Grouping**: Group by date في PHP بدلاً من SQL

### UX Enhancements

-   ✅ **Live Filters**: تحديث فوري عند تغيير الفلاتر
-   ✅ **Tooltips**: معلومات إضافية عند Hover
-   ✅ **Click Interaction**: فلترة مباشرة من التقويم
-   ✅ **Clear Indicators**: Banners واضحة للفلاتر النشطة
-   ✅ **Empty States**: رسائل واضحة عند عدم وجود بيانات

### Responsive Design

-   ✅ **Mobile-First**: التصميم يعمل بشكل مثالي على الجوال
-   ✅ **Grid Layout**: البطاقات تتكيف مع حجم الشاشة
-   ✅ **Scrollable Table**: الجداول قابلة للتمرير أفقياً
-   ✅ **Touch-Friendly**: التقويم قابل للنقر على الأجهزة اللمسية

### Accessibility

-   ✅ **Color Coding**: استخدام ألوان واضحة ومميزة
-   ✅ **Icons**: أيقونات توضيحية مع كل حالة
-   ✅ **Arabic Support**: دعم كامل للعربية واتجاه RTL
-   ✅ **Dark Mode**: دعم كامل للوضع الداكن

## الملفات المرتبطة

### Backend

-   `app/Livewire/Dashboard/StudentProfile/StudentAttendance.php`

### Frontend

-   `resources/views/livewire/dashboard/student-profile/student-attendance.blade.php`

### Models Used

-   `App\Models\Student`
-   `App\Models\Attendance`
-   `App\Models\ClassSession`
-   `App\Models\TimetableSlot`
-   `App\Models\Subject`
-   `App\Models\TeacherAssignment`

### Enums Used

-   `App\Enums\AttendanceStatusEnum`

## استكشاف الأخطاء

### المشكلة: الإحصائيات تظهر 0

**السبب**: لا توجد سجلات حضور في الفترة المحددة  
**الحل**: تحقق من وجود سجلات حضور للطالب

### المشكلة: التقويم فارغ

**السبب**: لم يتم تسجيل حضور في هذا الشهر  
**الحل**: انتقل لشهر آخر أو سجل بعض الحضور

### المشكلة: السجل التفصيلي فارغ

**السبب**: قد يكون هناك فلاتر نشطة  
**الحل**: استخدم زر "إعادة تعيين" أو تحقق من الفلاتر

### المشكلة: النقر على التقويم لا يعمل

**السبب**: قد تكون تنقر على أيام من شهر سابق/لاحق  
**الحل**: انقر على أيام من الشهر المعروض فقط

## التحسينات المستقبلية المقترحة

1. **تصدير PDF**: تصدير سجل الحضور كملف PDF
2. **رسوم بيانية**: إضافة Charts لتوضيح الاتجاهات
3. **مقارنة الطلاب**: مقارنة حضور الطالب مع معدل الصف
4. **إشعارات**: تنبيهات للغياب المتكرر
5. **تقارير دورية**: ملخص شهري يُرسل لولي الأمر
6. **Excuses Upload**: رفع ملفات الأعذار المرضية
7. **Attendance Goals**: أهداف للحضور مع progress bar

## Notes

-   التقويم يبدأ من الأحد (أول يوم في الأسبوع بالتقويم السعودي)
-   عطلة نهاية الأسبوع: الجمعة والسبت
-   جميع التواريخ معروضة بالتقويم الميلادي
-   الوقت الافتراضي للسجل التفصيلي: آخر 30 يوم
